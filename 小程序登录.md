## [微信小程序开放平台登录](https://developers.weixin.qq.com/platform/login)

^[1](https://developers.weixin.qq.com/platform/login)^^[2](https://blog.csdn.net/weixin_71403100/article/details/135873888)^^[3](https://blog.csdn.net/weixin_47803327/article/details/147301032)^

[](https://developers.weixin.qq.com/platform/login)

[](https://blog.csdn.net/weixin_71403100/article/details/135873888)

[](https://blog.csdn.net/weixin_47803327/article/details/147301032)

微信小程序的登录功能通过与微信开放平台的接口交互，实现用户身份验证和数据管理。以下是实现微信小程序登录的完整流程。

步骤 1：获取临时登录凭证 (code)

在小程序前端调用 *wx.login()* 获取临时登录凭证 *code*。此凭证有效期为5分钟且只能使用一次。

wx.login({

success: (res) => {

**if** (res.code) {

console.log(**'临时登录凭证:'**, res.code);

**// 将 code 发送到开发者服务器**

}

},

fail: (err) => {

console.error(**'登录失败:'**, err);

}

});

![复制]()

步骤 2：通过服务器换取 OpenID 和 Session Key

开发者服务器使用 *code* 调用微信接口 *https://api.weixin.qq.com/sns/jscode2session*，获取用户的 *OpenID* 和 *session\_key*。

**请求参数：**

* *appid*: 小程序的 AppID
* *secret*: 小程序的 AppSecret
* *js\_code*: 前端获取的 code
* *grant\_type*: 固定值为 *authorization\_code*

**示例代码 (Node.js)：**

**const** axios = **require**(**'axios'**);

async **function** getSessionKey(code) {

**const** response = await axios.**get**(**'https://api.weixin.qq.com/sns/jscode2session'**, {

params: {

appid: **'你的AppID'**,

secret: **'你的AppSecret'**,

js\_code: code,

grant\_type: **'authorization\_code'**

}

});

**return** response.data; **// 包含 openid 和 session\_key**

}

![复制]()

步骤 3：生成自定义登录态 (Token)

服务器生成一个自定义 Token，将其与用户的 OpenID 和 Session Key 关联存储，并返回给小程序。

**示例代码：**

**const** jwt = **require**(**'jsonwebtoken'**);

**function** generateToken(openid) {

**return** jwt.sign({ openid }, **'服务器密钥'**, { expiresIn: **'7d'** });

}

![复制]()

步骤 4：小程序存储 Token 并验证登录态

将服务器返回的 Token 存储在本地，后续请求时携带该 Token 验证用户身份。

wx.setStorageSync(**'auth\_token'**, token);

**// 请求示例**

wx.request({

url: **'https://your-server.com/api/data'**,

method: **'GET'**,

header: {

Authorization: **'Bearer '** + wx.getStorageSync(**'auth\_token'**)

},

success: (res) => {

console.log(**'数据请求成功:'**, res.data);

}

});

![复制]()

最佳实践与注意事项

1. **安全性**： 永远不要在前端存储或暴露 AppSecret。 使用 HTTPS 加密所有通信。
2. **Token 管理**： 设置合理的过期时间（如7天）。 提供刷新 Token 的机制。
3. **异常处理**： 针对 code 超时或接口调用失败提供友好的重新登录机制。

通过以上流程，您可以实现安全、高效的微信小程序开放平台登录功能。
